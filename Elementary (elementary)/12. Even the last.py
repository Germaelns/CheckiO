def checkio(array):
    if len(array) == 0:
        return 0
    elif len:
        num = 0
        for item in range(0, len(array), 2):
            num += array[item]
        return num * array[-1]


checkio([0, 1, 2, 3, 4, 5])
